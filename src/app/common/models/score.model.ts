export class Score{

public name : string;
public score : number;
public cardType : string;

constructor(name : string, score : number, cardType : string){
    this.name = name,
    this.score = score,
    this.cardType = cardType
}

}